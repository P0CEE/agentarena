{
  "name": "agentarena",
  "private": true,
  "packageManager": "bun@1.2.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "uv run ruff check packages"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
